
// Optional: Add animations or interactions later
document.querySelectorAll('.feature-card').forEach(card => {
    card.addEventListener('click', () => {
        alert('Coming Soon! 💖');
    });
});
